name: alphax_http
description: package:http Client bridge backed by AlphaXClient for Chopper, GraphQL HTTP, and injectable SDKs.
version: 1.0.0
homepage: https://github.com/auvana-ventures/alphax
repository: https://github.com/auvana-ventures/alphax
issue_tracker: https://github.com/auvana-ventures/alphax/issues
topics:
  - networking
  - http
  - interoperability
resolution: workspace
environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  alphax: ^1.0.0
  http: ^1.6.0

dev_dependencies:
  alphax_test: ^1.0.0
  lints: ^5.1.1
  test: ^1.25.0
