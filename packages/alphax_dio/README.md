# alphax_dio

Planned Dio `HttpClientAdapter` compatibility layer for AlphaX.

Phase 0 only documents the intended adoption path. The adapter will be implemented
after the selected transport supports the required request/response lifecycle,
cancellation, streams, progress, and body semantics. This package is not published
during Phase 0.
