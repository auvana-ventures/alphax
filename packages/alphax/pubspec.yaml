name: alphax
description: Cross-platform HTTP client API for Dart and Flutter with streaming, retries, caching, protocol control, middleware, and native transport integrations.
version: 1.0.0
homepage: https://github.com/auvana-ventures/alphax
repository: https://github.com/auvana-ventures/alphax
issue_tracker: https://github.com/auvana-ventures/alphax/issues
topics:
  - http
  - networking
  - middleware
  - streaming
  - websocket
resolution: workspace
environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.0
