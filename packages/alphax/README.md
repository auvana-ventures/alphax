# alphax

Pure-Dart, transport-independent request, response, cancellation, metrics, error,
and streaming contracts for AlphaX.

This package has no Flutter SDK dependency and is experimental during Phase 0. It
is not published to pub.dev until AlphaX naming clearance and architecture review
are complete.
