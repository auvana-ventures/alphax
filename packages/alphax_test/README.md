# alphax_test

Deterministic fake transports, responses, streams, delays, failures, cancellation,
file fixtures, and reusable transport-conformance tests for AlphaX contract tests.

The helpers do not require Flutter or a native engine and are intentionally usable
from pure Dart test suites. Adapter packages can run the same conformance suite
against their transport implementations.
