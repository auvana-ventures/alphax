# alphax_test

Deterministic fake transports, responses, streams, delays, failures, and
cancellation helpers for ordinary AlphaX application tests.

The helpers do not require a native engine and are intentionally usable from pure
Dart test suites.
