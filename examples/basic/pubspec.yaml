name: alphax_basic_example
description: Small AlphaX API example.
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  alphax: ^1.0.0
  alphax_native: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
