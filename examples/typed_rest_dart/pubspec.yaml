name: alphax_typed_rest_dart_example
description: Compile-tested pure-Dart consumer for the direct AlphaX typed REST generator.
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  alphax:
    path: ../../packages/alphax

dev_dependencies:
  alphax_generator:
    path: ../../packages/alphax_generator
  alphax_test:
    path: ../../packages/alphax_test
  build_runner: ^2.16.0
  lints: ^5.1.1
  test: ^1.31.1

dependency_overrides:
  alphax:
    path: ../../packages/alphax
