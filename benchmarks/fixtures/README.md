# Benchmark Fixtures

Deterministic payload or fixture files used by benchmark scenarios belong here.
Generated output should remain outside version control unless it is a reviewed,
intentional fixture.
