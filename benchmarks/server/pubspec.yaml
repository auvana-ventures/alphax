name: alphax_benchmark_server
description: Deterministic local server for AlphaX Phase 0 transport benchmarks.
publish_to: none
environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^5.1.1
  test: ^1.31.0
