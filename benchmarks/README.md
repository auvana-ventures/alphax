# Benchmarks

This directory contains the deterministic server, candidate client conventions,
scenario definitions, runner scripts, fixtures, and reviewed raw results for the
Phase 0 transport comparison. See [docs/benchmarks.md](../docs/benchmarks.md).
