# Benchmarks

This directory contains the deterministic server, benchmark-only transport
contract, candidate adapters, scenario runner, scripts, fixtures, and reviewed raw
results for the Phase 0 transport comparison. The runner gates performance on
correctness and reports scenarios independently; it does not select a production
transport. See [docs/benchmarks.md](../docs/benchmarks.md).
