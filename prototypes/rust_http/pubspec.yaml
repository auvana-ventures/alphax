name: alphax_rust_http_ffi_prototype
description: Local Dart FFI harness for the AlphaX Rust transport prototype.
publish_to: none
environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  alphax_benchmark_client:
    path: ../../benchmarks/client
  ffi: ^2.1.3

dev_dependencies:
  lints: ^5.1.1
  test: ^1.31.0
