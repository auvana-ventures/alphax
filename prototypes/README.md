# Transport prototypes

Phase 0 compares a `dart:io` baseline, libcurl through a small C ABI/FFI bridge,
and Rust reqwest/hyper through both a direct CLI and a small C ABI/FFI entry point.
None is a production transport decision yet.
