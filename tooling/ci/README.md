# CI tooling

Repository-level CI configuration belongs here when it cannot be expressed directly
in `.github/workflows/`. Keep performance benchmarks off shared CI unless the runner
is controlled and stable.
